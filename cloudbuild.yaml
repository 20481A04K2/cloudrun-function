steps:
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - run
      - deploy
      - discover-ad-status
      - --source=.
      - --image=us-east4-docker.pkg.dev/gcp-another-pro/cloud-run-source-deploy/discover-ad-status
      - --region=asia-southeast1
      - --project=gcp-another-pro
      - --allow-unauthenticated
      - --memory=512Mi
      - --timeout=300s
      - --max-instances=2

options:
  logging: CLOUD_LOGGING_ONLY
